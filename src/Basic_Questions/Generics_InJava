📌 Day 98: Java Generics
🔹 What are Generics?

Generics in Java allow you to write type-safe and reusable code by enabling classes,
interfaces, and methods to operate on objects of various types while providing compile-time type checking.

👉 Without generics, you would use Object type and then cast manually (risk of runtime errors).

🔹 Benefits of Generics

Type Safety – Prevents ClassCastException.
Code Reusability – Works with any object type.
Compile-time Checking – Errors caught early.
Eliminates Casting – No need to explicitly cast objects.


🚀 Java Generics with Methods

Generics in Java allow you to write reusable code that works with different data types without casting.

✅ Syntax of a Generic Method:
public <T> void printData(T data) {
    System.out.println(data);
}
<T> → Type parameter (can be anything: T, E, K, V, etc.)
T data → Method can accept any type.
printData(10) → Integer
printData("Hello") → String

🔑 Key Points:

Generic methods are type-safe.
They reduce code duplication.
extends Comparable<T> is used for bounds (restrict type to comparable objects).



🔹 What are Wildcards in Generics?

In Java Generics, the wildcard ? represents an unknown type.
It’s useful when you don’t know in advance what type a collection will hold, but still want to write flexible code.

🔹 Types of Wildcards
Unbounded Wildcard <?>
Accepts any type.
Example: List<?> → list of unknown objects.

Upper Bounded Wildcard <? extends T>
Accepts type T or its subclasses.
Example: List<? extends Number> → can hold Integer, Double, etc.

Lower Bounded Wildcard <? super T>
Accepts type T or its superclasses.
Example: List<? super Integer> → can hold Integer, Number, Object.